[package]
name = "policy-checker"
version = "0.1.0"
edition = "2021"

[dependencies]
ureq = { version = "2", features = ["native-tls"] }
serde_json = "1"
serde = { version = "1", features = ["derive"] }
regex = "1"
